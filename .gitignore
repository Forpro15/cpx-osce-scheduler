# ⚠️ 개인정보 보호: 실제 학생 데이터가 담긴 파일은 커밋하지 않습니다.
# 아래 확장자/이름은 기본적으로 추적에서 제외합니다.

# 실제 배정 워크북 (학생 명단·점수 포함 가능)
*.xlsx
*.xlsm
*.xls
*.csv
*.tsv

# 단, 개인정보 없는 참조 템플릿은 예외로 추적하려면 아래처럼 강제 포함
# !templates/*.example.xlsx

# 학교별 실제 설정 (개인정보는 없지만 내부용일 수 있음)
config/schedule.yaml

# 임시/캐시
.DS_Store
Thumbs.db
~$*
*.tmp
